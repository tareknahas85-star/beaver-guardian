<?xml version="1.0" encoding="utf-8"?>
<!-- Amber banner used when something needs the parent's attention but is not an
     emergency — for example notifications being switched off. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="@color/tertiary_container" />
    <corners android:radius="12dp" />
    <stroke android:width="1dp" android:color="@color/tertiary" />
</shape>
