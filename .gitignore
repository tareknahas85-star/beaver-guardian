*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
app/build/

# The real google-services.json is stored as the GOOGLE_SERVICES_JSON_B64 repo secret
# (restored by CI before every build) and never committed. Copy app/google-services.json.example
# to app/google-services.json locally and fill in your own Firebase project's values.
app/google-services.json
